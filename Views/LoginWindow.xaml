<Window x:Class="SmartInventoryTracker.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartInventoryTracker.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="350" Width="650"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#FF2D2D30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Smart Inventory Tracker" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="White" HorizontalAlignment="Center"
                   Margin="0,20,0,40"/>

        <StackPanel Grid.Row="1" Width="300" HorizontalAlignment="Center">
            <TextBlock Text="Username" Foreground="White" Margin="0,0,0,5"/>
            <TextBox Name="UsernameTextBox" Height="30" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Width="300" HorizontalAlignment="Center" Margin="0,15,0,0">
            <TextBlock Text="Password" Foreground="White" Margin="0,0,0,5"/>
            <PasswordBox Name="PasswordBox" Height="30" FontSize="14"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Login" Width="300" Height="35" 
                HorizontalAlignment="Center" Margin="0,25,0,0"
                Background="#FF007ACC" Foreground="White" FontWeight="Bold"
                Click="LoginButton_Click"/>
    </Grid>
</Window>
